# Optional override for Calendly (default in code is https://calendly.com/srihan-protent/demo)
# VITE_CALENDLY_DEMO_URL=

# Optional full-bleed hero MP4. If unset, the hero uses the built-in SVG (multi-feed + intelligence hub).
# VITE_HERO_VIDEO_URL=
