# Optional override for Calendly (default in code is https://calendly.com/srihan-protent/demo)
# VITE_CALENDLY_DEMO_URL=

# Optional full-bleed hero MP4 (overrides the default hero photograph).
# VITE_HERO_VIDEO_URL=

# Optional hero image URL (default /hero-soc.png in public/). Use for a different still or CDN path.
# VITE_HERO_IMAGE_URL=
